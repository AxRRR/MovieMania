import React from 'react';

export const Add = ({ addFilm }) => {
    console.log('El addfilm >>>>', addFilm)
    return (
        <div>
            <p>El array esta asi:</p>
            <p></p>
        </div>
    );
};
